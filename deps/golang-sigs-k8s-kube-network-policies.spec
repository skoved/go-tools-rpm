# Generated by go2rpm 1.16.0
%bcond check 1
%bcond bootstrap 0

%if %{with bootstrap}
%global debug_package %{nil}
%endif

%if %{with bootstrap}
%global __requires_exclude %{?__requires_exclude:%{__requires_exclude}|}^golang\\(.*\\)$
%endif

# https://github.com/kubernetes-sigs/kube-network-policies
%global goipath         sigs.k8s.io/kube-network-policies
%global forgeurl        https://github.com/kubernetes-sigs/kube-network-policies
Version:                0.8.0
%global tag             v0.8.0

# REMOVE BEFORE SUBMITTING THIS FOR REVIEW
# ---
# New Fedora packages should use %%gometa -f, which makes the package
# ExclusiveArch to %%golang_arches_future and thus excludes the package from
# %%ix86. If the new package is needed as a dependency for another package,
# please consider removing that package from %%ix86 in the same way, instead of
# building more go packages for i686. If your package is not a leaf package,
# you'll need to coordinate the removal of the package's dependents first.
# ---
# REMOVE BEFORE SUBMITTING THIS FOR REVIEW
%gometa -L -f

%global common_description %{expand:
Kubernetes network policies.}

%global golicenses      LICENSE
%global godocs          docs CONTRIBUTING.md README.md RELEASE.md SECURITY.md\\\
                        code-of-conduct.md

Name:           golang-sigs-k8s-kube-network-policies
Release:        %autorelease
Summary:        Kubernetes network policies

License:        Apache-2.0
URL:            %{gourl}
Source:         %{gosource}

Provides: golang(sigs.k8s.io/network-policy-api/apis/v1alpha1) = %{version}-%{release}
Provides: golang(sigs.k8s.io/network-policy-api/pkg/client/clientset/versioned) = %{version}-%{release}
Provides: golang(sigs.k8s.io/network-policy-api/pkg/client/clientset/versioned/fake) = %{version}-%{release}
Provides: golang(sigs.k8s.io/network-policy-api/pkg/client/informers/externalversions) = %{version}-%{release}
Provides: golang(sigs.k8s.io/network-policy-api/pkg/client/informers/externalversions/apis/v1alpha1) = %{version}-%{release}
Provides: golang(sigs.k8s.io/network-policy-api/pkg/client/listers/apis/v1alpha1) = %{version}-%{release}

%description %{common_description}

%gopkg

%prep
%goprep -A
%autopatch -p1

%if %{without bootstrap}
%generate_buildrequires
%go_generate_buildrequires
%endif

%if %{without bootstrap}
%build
%gobuild -o %{gobuilddir}/bin/kube-network-policies %{goipath}/cmd/main.go
%endif

%install
%gopkginstall
%if %{without bootstrap}
install -m 0755 -vd                     %{buildroot}%{_bindir}
install -m 0755 -vp %{gobuilddir}/bin/* %{buildroot}%{_bindir}/
%endif

%if %{without bootstrap}
%if %{with check}
%check
%gocheck
%endif
%endif

%if %{without bootstrap}
%files
%license LICENSE
%doc docs CONTRIBUTING.md README.md RELEASE.md SECURITY.md code-of-conduct.md
# REMOVE BEFORE SUBMITTING THIS FOR REVIEW
# ---
# New Fedora packages should not use globs to avoid installing conflicting
# binaries.
# Write a _bindir line per each of the binaries the package will install.
# ---
# REMOVE BEFORE SUBMITTING THIS FOR REVIEW
%{_bindir}/*
%endif

%gopkgfiles

%changelog
%autochangelog
